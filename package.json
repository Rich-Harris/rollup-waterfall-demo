{
  "name": "rollup-starter-app",
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "rollup": "^0.65.2",
    "serve": "^10.0.1"
  },
  "dependencies": {
    "date-fns": "^1.19.0"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  }
}
